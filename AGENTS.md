# Codex Project Instructions

Read repository-root `README.md`, both approved design specifications under `docs/superpowers/specs/`, and the implementation roadmap before changing code.
Execute only the active plan task, use an isolated branch/worktree, run its named gates, and submit exact evidence.
Run implementation and npm commands from `plugins/project-memory/`; run marketplace, Plugin, skill, architecture, and roadmap commands from the repository root.
Do not edit generated schemas, catalog locks, profile locks, generated views, or append-only history by hand.
The Plugin skill is the primary agent experience. The bundled CLI is deterministic internal machinery and a cross-tool fallback, not a manual profile picker.
Do not install the Plugin, publish, push, or modify a live product repository without separate Pitaji approval.
This file is a tool adapter, not canonical product truth.
