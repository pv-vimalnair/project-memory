overlay_definition:
  id: overlay.runtime.hosted
  version: 1.0.0
  status: active
  name: Runtime Hosted
  category: runtime
  purpose: Apply the runtime hosted trait without changing the enduring project root.
  inclusion_boundary: Include constraints and defaults caused specifically by the runtime hosted trait.
  exclusion_boundary: Do not use this overlay as a substitute for a blueprint, component, or temporary workstream.
  compatible_root_kinds:
    - product
    - shared-system
    - program
    - portfolio
    - engagement
  compatible_archetypes:
    - application-service
    - developer-platform
    - game-interactive
    - ai-data
    - commerce-network
    - content-learning
    - brand-design
    - research-knowledge
    - operations-automation
    - portfolio
    - engagement
  requires_overlays: []
  conflicts_with:
    - overlay.runtime.self-hosted
  default_components:
    - component.infrastructure
  default_domains:
    - domain.release-platform-operations
  required_documents:
    - docs/overlays/runtime-hosted.md
  required_records:
    - profile-lock
    - decision-log
  tags:
    - operating-shape
    - runtime
    - hosted
  positive_examples:
    - The project explicitly operates with the runtime hosted trait.
  negative_examples:
    - A one-off task resembles runtime hosted but does not change the project trait.
