overlay_definition:
  id: overlay.distribution.public-web
  version: 1.0.0
  status: active
  name: Distribution Public Web
  category: distribution
  purpose: Apply the distribution public web trait without changing the enduring project root.
  inclusion_boundary: Include constraints and defaults caused specifically by the distribution public web trait.
  exclusion_boundary: Do not use this overlay as a substitute for a blueprint, component, or temporary workstream.
  compatible_root_kinds:
    - product
    - shared-system
    - program
    - portfolio
    - engagement
  compatible_archetypes:
    - application-service
    - developer-platform
    - game-interactive
    - ai-data
    - commerce-network
    - content-learning
    - brand-design
    - research-knowledge
    - operations-automation
    - portfolio
    - engagement
  requires_overlays:
    - overlay.surface.web
  conflicts_with: []
  default_components:
    - component.distribution
  default_domains:
    - domain.release-platform-operations
  required_documents:
    - docs/overlays/distribution-public-web.md
  required_records:
    - profile-lock
    - decision-log
  tags:
    - operating-shape
    - distribution
    - public-web
  positive_examples:
    - The project explicitly operates with the distribution public web trait.
  negative_examples:
    - A one-off task resembles distribution public web but does not change the project trait.
