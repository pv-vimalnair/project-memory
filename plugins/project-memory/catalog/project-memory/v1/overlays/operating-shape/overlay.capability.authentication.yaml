overlay_definition:
  id: overlay.capability.authentication
  version: 1.0.0
  status: active
  name: Capability Authentication
  category: capability
  purpose: Apply the capability authentication trait without changing the enduring project root.
  inclusion_boundary: Include constraints and defaults caused specifically by the capability authentication trait.
  exclusion_boundary: Do not use this overlay as a substitute for a blueprint, component, or temporary workstream.
  compatible_root_kinds:
    - product
    - shared-system
    - program
    - portfolio
    - engagement
  compatible_archetypes:
    - application-service
    - developer-platform
    - game-interactive
    - ai-data
    - commerce-network
    - content-learning
    - brand-design
    - research-knowledge
    - operations-automation
    - portfolio
    - engagement
  requires_overlays: []
  conflicts_with: []
  default_components:
    - component.identity
  default_domains:
    - domain.engineering-architecture
  required_documents:
    - docs/overlays/capability-authentication.md
  required_records:
    - profile-lock
    - decision-log
  tags:
    - operating-shape
    - capability
    - authentication
  positive_examples:
    - The project explicitly operates with the capability authentication trait.
  negative_examples:
    - A one-off task resembles capability authentication but does not change the project trait.
