overlay_definition:
  id: overlay.runtime.realtime
  version: 1.0.0
  status: active
  name: Runtime Realtime
  category: runtime
  purpose: Apply the runtime realtime trait without changing the enduring project root.
  inclusion_boundary: Include constraints and defaults caused specifically by the runtime realtime trait.
  exclusion_boundary: Do not use this overlay as a substitute for a blueprint, component, or temporary workstream.
  compatible_root_kinds:
    - product
    - shared-system
    - program
    - portfolio
    - engagement
  compatible_archetypes:
    - application-service
    - developer-platform
    - game-interactive
    - ai-data
    - commerce-network
    - content-learning
    - brand-design
    - research-knowledge
    - operations-automation
    - portfolio
    - engagement
  requires_overlays: []
  conflicts_with: []
  default_components:
    - component.infrastructure
  default_domains:
    - domain.release-platform-operations
  required_documents:
    - docs/overlays/runtime-realtime.md
  required_records:
    - profile-lock
    - decision-log
  tags:
    - operating-shape
    - runtime
    - realtime
  positive_examples:
    - The project explicitly operates with the runtime realtime trait.
  negative_examples:
    - A one-off task resembles runtime realtime but does not change the project trait.
