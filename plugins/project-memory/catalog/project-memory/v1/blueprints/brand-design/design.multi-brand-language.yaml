blueprint:
  id: design.multi-brand-language
  version: 1.0.0
  status: active
  group_id: blueprint-group.brand-design
  allowed_root_kinds:
    - shared-system
  primary_archetype: brand-design
  purpose: Use when the enduring root is best described as design multi brand language, independent of its temporary workstreams.
  selection:
    feature_schema_version: 1.0.0
    required_signals:
      - id: design.multi-brand-language.required-root
        feature: root.kind
        operator: in
        expected:
          - shared-system
        evidence_required: true
    positive_signals:
      - id: design.multi-brand-language.positive-shape
        feature: product.shape
        operator: equals
        expected: design.multi-brand-language
        evidence_required: true
        weight: 100
    negative_signals:
      - id: design.multi-brand-language.negative-shape
        feature: product.anti-shapes
        operator: tag_present
        expected: design.multi-brand-language
        evidence_required: false
        penalty: 100
    exclusions:
      - id: design.multi-brand-language.explicit-exclusion
        feature: product.exclusions
        operator: tag_present
        expected: design.multi-brand-language
        evidence_required: false
    max_positive_weight: 100
    specificity_rank: 102
    precedence: 998
  overlays:
    baked: []
    defaults:
      - overlay.lifecycle.active
    forbidden: []
  default_components:
    - component.brand
    - component.visual-design
    - component.design-system
    - component.content-design
  default_domains:
    - domain.visual-brand-design-systems
    - domain.ux-content-design
  adapter_slots:
    - agent
    - runtime
    - workflow
  required_documents:
    - PROJECT.md
    - PRODUCT.md
    - ARCHITECTURE.md
  validation_gates:
    - root boundary confirmed
    - positive evidence recorded
    - anti-signals evaluated
  positive_examples:
    - A durable design multi brand language root containing many different workstreams.
  negative_examples:
    - A temporary design multi brand language-related deliverable inside another enduring product.
  name: Design Multi Brand Language
  inclusion_boundary: Include the enduring capabilities and governance of the design multi brand language root.
  exclusion_boundary: Exclude short-lived campaigns, audits, refactors, and checks; represent those as workstreams.
