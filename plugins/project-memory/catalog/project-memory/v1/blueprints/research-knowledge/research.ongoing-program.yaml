blueprint:
  id: research.ongoing-program
  version: 1.0.0
  status: active
  group_id: blueprint-group.research-knowledge
  allowed_root_kinds:
    - program
    - shared-system
  primary_archetype: research-knowledge
  purpose: Use when the enduring root is best described as research ongoing program, independent of its temporary workstreams.
  selection:
    feature_schema_version: 1.0.0
    required_signals:
      - id: research.ongoing-program.required-root
        feature: root.kind
        operator: in
        expected:
          - program
          - shared-system
        evidence_required: true
    positive_signals:
      - id: research.ongoing-program.positive-shape
        feature: product.shape
        operator: equals
        expected: research.ongoing-program
        evidence_required: true
        weight: 100
    negative_signals:
      - id: research.ongoing-program.negative-shape
        feature: product.anti-shapes
        operator: tag_present
        expected: research.ongoing-program
        evidence_required: false
        penalty: 100
    exclusions:
      - id: research.ongoing-program.explicit-exclusion
        feature: product.exclusions
        operator: tag_present
        expected: research.ongoing-program
        evidence_required: false
    max_positive_weight: 100
    specificity_rank: 100
    precedence: 1000
  overlays:
    baked: []
    defaults:
      - overlay.lifecycle.active
    forbidden: []
  default_components:
    - component.research-program
    - component.knowledge-base
    - component.user-research
    - component.reporting
  default_domains:
    - domain.research-insight
    - domain.data-analytics-ai
  adapter_slots:
    - agent
    - runtime
    - workflow
  required_documents:
    - PROJECT.md
    - PRODUCT.md
    - ARCHITECTURE.md
  validation_gates:
    - root boundary confirmed
    - positive evidence recorded
    - anti-signals evaluated
  positive_examples:
    - A durable research ongoing program root containing many different workstreams.
  negative_examples:
    - A temporary research ongoing program-related deliverable inside another enduring product.
  name: Research Ongoing Program
  inclusion_boundary: Include the enduring capabilities and governance of the research ongoing program root.
  exclusion_boundary: Exclude short-lived campaigns, audits, refactors, and checks; represent those as workstreams.
