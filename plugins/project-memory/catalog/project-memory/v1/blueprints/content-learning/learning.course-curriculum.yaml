blueprint:
  id: learning.course-curriculum
  version: 1.0.0
  status: active
  group_id: blueprint-group.content-learning
  allowed_root_kinds:
    - product
    - shared-system
  primary_archetype: content-learning
  purpose: Use when the enduring root is best described as learning course curriculum, independent of its temporary workstreams.
  selection:
    feature_schema_version: 1.0.0
    required_signals:
      - id: learning.course-curriculum.required-root
        feature: root.kind
        operator: in
        expected:
          - product
          - shared-system
        evidence_required: true
    positive_signals:
      - id: learning.course-curriculum.positive-shape
        feature: product.shape
        operator: equals
        expected: learning.course-curriculum
        evidence_required: true
        weight: 100
    negative_signals:
      - id: learning.course-curriculum.negative-shape
        feature: product.anti-shapes
        operator: tag_present
        expected: learning.course-curriculum
        evidence_required: false
        penalty: 100
    exclusions:
      - id: learning.course-curriculum.explicit-exclusion
        feature: product.exclusions
        operator: tag_present
        expected: learning.course-curriculum
        evidence_required: false
    max_positive_weight: 100
    specificity_rank: 103
    precedence: 997
  overlays:
    baked: []
    defaults:
      - overlay.lifecycle.active
    forbidden: []
  default_components:
    - component.editorial-content
    - component.content-design
    - component.localization
    - component.knowledge-base
  default_domains:
    - domain.content-communications-localization
    - domain.research-insight
  adapter_slots:
    - agent
    - runtime
    - workflow
  required_documents:
    - PROJECT.md
    - PRODUCT.md
    - ARCHITECTURE.md
  validation_gates:
    - root boundary confirmed
    - positive evidence recorded
    - anti-signals evaluated
  positive_examples:
    - A durable learning course curriculum root containing many different workstreams.
  negative_examples:
    - A temporary learning course curriculum-related deliverable inside another enduring product.
  name: Learning Course Curriculum
  inclusion_boundary: Include the enduring capabilities and governance of the learning course curriculum root.
  exclusion_boundary: Exclude short-lived campaigns, audits, refactors, and checks; represent those as workstreams.
