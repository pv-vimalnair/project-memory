blueprint:
  id: program.organization-capability
  version: 1.0.0
  status: active
  group_id: blueprint-group.portfolio
  allowed_root_kinds:
    - portfolio
    - program
  primary_archetype: portfolio
  purpose: Use when the enduring root is best described as program organization capability, independent of its temporary workstreams.
  selection:
    feature_schema_version: 1.0.0
    required_signals:
      - id: program.organization-capability.required-root
        feature: root.kind
        operator: in
        expected:
          - portfolio
          - program
        evidence_required: true
    positive_signals:
      - id: program.organization-capability.positive-shape
        feature: product.shape
        operator: equals
        expected: program.organization-capability
        evidence_required: true
        weight: 100
    negative_signals:
      - id: program.organization-capability.negative-shape
        feature: product.anti-shapes
        operator: tag_present
        expected: program.organization-capability
        evidence_required: false
        penalty: 100
    exclusions:
      - id: program.organization-capability.explicit-exclusion
        feature: product.exclusions
        operator: tag_present
        expected: program.organization-capability
        evidence_required: false
    max_positive_weight: 100
    specificity_rank: 104
    precedence: 996
  overlays:
    baked: []
    defaults:
      - overlay.lifecycle.active
    forbidden: []
  default_components:
    - component.product-management
    - component.shared-platform
    - component.brand
  default_domains:
    - domain.governance-coordination
    - domain.product-strategy
    - domain.engineering-architecture
  adapter_slots:
    - agent
    - runtime
    - workflow
  required_documents:
    - PROJECT.md
    - PRODUCT.md
    - ARCHITECTURE.md
  validation_gates:
    - root boundary confirmed
    - positive evidence recorded
    - anti-signals evaluated
  positive_examples:
    - A durable program organization capability root containing many different workstreams.
  negative_examples:
    - A temporary program organization capability-related deliverable inside another enduring product.
  name: Program Organization Capability
  inclusion_boundary: Include the enduring capabilities and governance of the program organization capability root.
  exclusion_boundary: Exclude short-lived campaigns, audits, refactors, and checks; represent those as workstreams.
