blueprint:
  id: game.simulation-learning
  version: 1.0.0
  status: active
  group_id: blueprint-group.game-interactive
  allowed_root_kinds:
    - product
  primary_archetype: game-interactive
  purpose: Use when the enduring root is best described as game simulation learning, independent of its temporary workstreams.
  selection:
    feature_schema_version: 1.0.0
    required_signals:
      - id: game.simulation-learning.required-root
        feature: root.kind
        operator: in
        expected:
          - product
        evidence_required: true
    positive_signals:
      - id: game.simulation-learning.positive-shape
        feature: product.shape
        operator: equals
        expected: game.simulation-learning
        evidence_required: true
        weight: 100
    negative_signals:
      - id: game.simulation-learning.negative-shape
        feature: product.anti-shapes
        operator: tag_present
        expected: game.simulation-learning
        evidence_required: false
        penalty: 100
    exclusions:
      - id: game.simulation-learning.explicit-exclusion
        feature: product.exclusions
        operator: tag_present
        expected: game.simulation-learning
        evidence_required: false
    max_positive_weight: 100
    specificity_rank: 104
    precedence: 996
  overlays:
    baked: []
    defaults:
      - overlay.lifecycle.active
      - overlay.audience.consumer
    forbidden: []
  default_components:
    - component.game-design
    - component.game-runtime
    - component.game-art
    - component.game-audio
  default_domains:
    - domain.game-design-art-audio-live-operations
    - domain.qa-reliability-performance
  adapter_slots:
    - agent
    - runtime
    - workflow
  required_documents:
    - PROJECT.md
    - PRODUCT.md
    - ARCHITECTURE.md
  validation_gates:
    - root boundary confirmed
    - positive evidence recorded
    - anti-signals evaluated
  positive_examples:
    - A durable game simulation learning root containing many different workstreams.
  negative_examples:
    - A temporary game simulation learning-related deliverable inside another enduring product.
  name: Game Simulation Learning
  inclusion_boundary: Include the enduring capabilities and governance of the game simulation learning root.
  exclusion_boundary: Exclude short-lived campaigns, audits, refactors, and checks; represent those as workstreams.
