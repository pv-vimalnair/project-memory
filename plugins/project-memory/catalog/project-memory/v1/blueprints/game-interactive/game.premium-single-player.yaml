blueprint:
  id: game.premium-single-player
  version: 1.0.0
  status: active
  group_id: blueprint-group.game-interactive
  allowed_root_kinds:
    - product
  primary_archetype: game-interactive
  purpose: Use when the enduring root is best described as game premium single player, independent of its temporary workstreams.
  selection:
    feature_schema_version: 1.0.0
    required_signals:
      - id: game.premium-single-player.required-root
        feature: root.kind
        operator: in
        expected:
          - product
        evidence_required: true
    positive_signals:
      - id: game.premium-single-player.positive-shape
        feature: product.shape
        operator: equals
        expected: game.premium-single-player
        evidence_required: true
        weight: 100
    negative_signals:
      - id: game.premium-single-player.negative-shape
        feature: product.anti-shapes
        operator: tag_present
        expected: game.premium-single-player
        evidence_required: false
        penalty: 100
    exclusions:
      - id: game.premium-single-player.explicit-exclusion
        feature: product.exclusions
        operator: tag_present
        expected: game.premium-single-player
        evidence_required: false
    max_positive_weight: 100
    specificity_rank: 100
    precedence: 1000
  overlays:
    baked: []
    defaults:
      - overlay.lifecycle.active
      - overlay.audience.consumer
    forbidden: []
  default_components:
    - component.game-design
    - component.game-runtime
    - component.game-art
    - component.game-audio
  default_domains:
    - domain.game-design-art-audio-live-operations
    - domain.qa-reliability-performance
  adapter_slots:
    - agent
    - runtime
    - workflow
  required_documents:
    - PROJECT.md
    - PRODUCT.md
    - ARCHITECTURE.md
  validation_gates:
    - root boundary confirmed
    - positive evidence recorded
    - anti-signals evaluated
  positive_examples:
    - A durable game premium single player root containing many different workstreams.
  negative_examples:
    - A temporary game premium single player-related deliverable inside another enduring product.
  name: Game Premium Single Player
  inclusion_boundary: Include the enduring capabilities and governance of the game premium single player root.
  exclusion_boundary: Exclude short-lived campaigns, audits, refactors, and checks; represent those as workstreams.
