blueprint:
  id: developer.api-product
  version: 1.0.0
  status: active
  group_id: blueprint-group.developer-platform
  allowed_root_kinds:
    - product
    - shared-system
  primary_archetype: developer-platform
  purpose: Use when the enduring root is best described as developer api product, independent of its temporary workstreams.
  selection:
    feature_schema_version: 1.0.0
    required_signals:
      - id: developer.api-product.required-root
        feature: root.kind
        operator: in
        expected:
          - product
          - shared-system
        evidence_required: true
    positive_signals:
      - id: developer.api-product.positive-shape
        feature: product.shape
        operator: equals
        expected: developer.api-product
        evidence_required: true
        weight: 100
    negative_signals:
      - id: developer.api-product.negative-shape
        feature: product.anti-shapes
        operator: tag_present
        expected: developer.api-product
        evidence_required: false
        penalty: 100
    exclusions:
      - id: developer.api-product.explicit-exclusion
        feature: product.exclusions
        operator: tag_present
        expected: developer.api-product
        evidence_required: false
    max_positive_weight: 100
    specificity_rank: 100
    precedence: 1000
  overlays:
    baked: []
    defaults:
      - overlay.lifecycle.active
      - overlay.surface.web
      - overlay.surface.api
      - overlay.audience.developer
    forbidden: []
  default_components:
    - component.api
    - component.sdk
    - component.cli
    - component.shared-platform
  default_domains:
    - domain.engineering-architecture
    - domain.release-platform-operations
  adapter_slots:
    - agent
    - runtime
    - workflow
  required_documents:
    - PROJECT.md
    - PRODUCT.md
    - ARCHITECTURE.md
  validation_gates:
    - root boundary confirmed
    - positive evidence recorded
    - anti-signals evaluated
  positive_examples:
    - A durable developer api product root containing many different workstreams.
  negative_examples:
    - A temporary developer api product-related deliverable inside another enduring product.
  name: Developer Api Product
  inclusion_boundary: Include the enduring capabilities and governance of the developer api product root.
  exclusion_boundary: Exclude short-lived campaigns, audits, refactors, and checks; represent those as workstreams.
