adapter_definition:
  id: adapter.python-data-ai
  version: 1.0.0
  status: active
  name: Python Data Ai
  kind: runtime
  purpose: Translate canonical Project Memory context for Python Data Ai without duplicating source-of-truth facts.
  inclusion_boundary: Include detection, commands, gates, conventions, and handoff fields specific to Python Data Ai.
  exclusion_boundary: Exclude canonical product decisions and records that belong in Project Memory itself.
  compatible_root_kinds:
    - product
    - shared-system
    - program
    - portfolio
    - engagement
  compatible_archetypes:
    - application-service
    - developer-platform
    - game-interactive
    - ai-data
    - commerce-network
    - content-learning
    - brand-design
    - research-knowledge
    - operations-automation
    - portfolio
    - engagement
  detection_signals:
    - pyproject.toml
    - Python sources
  relevant_files:
    - pyproject.toml
    - requirements.txt
  repository_conventions:
    - Read canonical Project Memory before work
    - Write only within an accepted task scope
  supported_commands:
    - inspect project context
    - validate task evidence
    - prepare a completion packet
  validation_gates:
    - adapter inputs resolve
    - canonical records are not duplicated
    - handoff is evidence linked
  default_components:
    - component.configuration
  default_domains:
    - domain.engineering-architecture
  required_documents:
    - docs/adapters/python-data-ai.md
  required_records:
    - worklog
    - evidence
    - handoff
  risks:
    - stale generated adapter view
    - tool-specific text diverges from canonical memory
  handoff_fields:
    - task identifier
    - base revision
    - scope
    - evidence
    - remaining risks
  tags:
    - runtime
    - python-data-ai
  positive_examples:
    - The repository contains verified Python Data Ai signals and the task uses that tool.
  negative_examples:
    - The task mentions Python Data Ai but the repository has no matching runtime or workflow.
